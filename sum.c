#include <stdio.h>
int main()
{ 
  // this is my the sum program of two number!
  int a, b, sum;
  printf("\nEnter two no: ");
  scanf("%d %d", &a, &b);
  sum = a + b;
  printf("Sum : %d", sum);
  return (0);
}