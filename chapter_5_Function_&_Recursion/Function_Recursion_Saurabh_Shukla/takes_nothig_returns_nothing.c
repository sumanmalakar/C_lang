#include<stdio.h> 

// Takes Nothing Returns Nothing 

void sum();
 int main(){


return 0;
}

void sum()
{
  int a, b, c;
  printf("Enter the two number\n");
  scanf("%d %d", &a, &b);
  printf("sum is %d", c = a + b);
}